'use client';

import React from 'react';

interface ChatPanelProps {
  ticketId: number | string;
}

export function ChatPanel({ ticketId }: ChatPanelProps) {
  // Live Chat is temporarily disabled/removed per user requirements
  return null;
}
